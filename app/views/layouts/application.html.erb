<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "actiontext", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="flex flex-col">
    <div class="flex flex-row justify-between items-center text-yellow-300 p-2 w-full h-16 bg-red-500">
      <div class="flex flex-col">
        <h1 class="text-3xl">Nice Blog</h1>
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>
      <% if user_signed_in? %>
        <div class="flex gap-3 items-center">
          <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "bg-yellow-300 text-red-500 p-2 rounded-lg hover:bg-yellow-200 transition-colors" %>
        </div>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
